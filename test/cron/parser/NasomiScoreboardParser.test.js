import { describe } from 'mocha';

describe('NasomiScoreboardParser', () => {
  describe('parse', () => {
    it('should return an empty set of records if the page was not parsable for some reason, given something malformed', () => {});

    it('should refuse to parse the output if the element passed is in undefined or null', () => {});

    it('should parse into a set of monster records given some HTML output that is valid', () => {});
  });
});
